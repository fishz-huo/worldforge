{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "WorldForge 默认权限：核心能力 + 系统「另存为」对话框。数据始终存在本机 IndexedDB，文件写入由应用自己的 save_file 命令完成，因此不需要文件系统权限。",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
